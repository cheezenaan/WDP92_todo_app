<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>
      <ul>
      <% @task.erros.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label      :content %>
    <%= f.text_area  :content %><br>
  </div>
  <div class="field">
    <%= f.label   :status %>
    <%= f.select  :status, Task.statuses.keys.to_a %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>